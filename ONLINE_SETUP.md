# OWJ Online Backend

این نسخه یک Backend واقعی Node/Express دارد:

- ثبت‌نام بازیکن از گوشی با نام و عکس
- ذخیره عکس روی سرور در `server-data/players`
- ذخیره درخواست‌ها در `server-data/registrations.json`
- پنل مدیریت برای مشاهده درخواست‌های جدید
- تأیید/رد بازیکن
- ساخت خودکار Player ID و PIN هنگام تأیید
- به‌روزرسانی پنل مدیریت هر ۵ ثانیه
- سرویس‌دهی خود پروژه از `dist` توسط Node

## اجرای محلی

```bash
npm install
npm run build
OWJ_ADMIN_PASSWORD='یک-رمز-قوی' OWJ_SESSION_SECRET='یک-رشته-تصادفی-طولانی' npm start
```

سپس سایت روی پورت `3000` باز می‌شود.

## انتشار آنلاین

یک سرور Node.js/VPS یا هاست Node.js لازم است. فایل‌های پروژه را روی سرور قرار دهید، `npm install` و `npm run build` را اجرا کنید و متغیرهای محیطی زیر را تنظیم کنید:

- `OWJ_ADMIN_PASSWORD`
- `OWJ_SESSION_SECRET`
- `PORT`

برای ماندگاری اطلاعات، پوشه `server-data` باید روی دیسک دائمی سرور باشد و بکاپ شود.

## نکته امنیتی

رمز مدیریت فقط در متغیر محیطی سرور قرار می‌گیرد و داخل React یا فایل‌های public قرار داده نشده است. در محیط واقعی حتماً HTTPS، رمز قوی و بکاپ منظم فعال کنید.
